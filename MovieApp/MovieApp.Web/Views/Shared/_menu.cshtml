@model List<Genre>


<div class="list-group">
   
    @foreach(var genre in Model)
    {
        <a href="#" class="list-group-item list-group-item-action disabled" tabindex="-1" aria-disabled="true">@genre.Name</a>
    }

   
</div>